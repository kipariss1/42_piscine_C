CC = gcc
CFLAGS = -Wall
NAME = do_op
INCLUDE_DIR = -I../../libs/libft/includes
LIB_DIR = -L../../libs/libft/
LIB_NAME = -lft

all:
	$(CC) $(CFLAGS) ./do_op.c -o $(NAME) $(INCLUDE_DIR) $(LIB_DIR) $(LIB_NAME)

clean: 
	rm $(NAME)